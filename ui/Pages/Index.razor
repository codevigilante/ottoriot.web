@page "/"
@inject MetaService Meta

@if (@Model != null)
{
    <PredictionsView Season="@Model.Season" Week="@Model.Week" />
}

@code {
    private MetaModel Model = null;

    protected override async Task OnInitializedAsync()
    {
        Model = await Meta.Fetch();
    }
}