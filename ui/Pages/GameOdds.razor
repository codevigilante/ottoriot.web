@page "/game-odds"
@inject MetaService Meta

@if (Model != null)
{
    <GameOddsView Season="@Model.Season" Week="@Model.Week" />
}

@code {
    private MetaModel Model = null;

    protected override async Task OnInitializedAsync()
    {
        Model = await Meta.Fetch();

        Console.WriteLine($"dingus {Model}");
    }
}

